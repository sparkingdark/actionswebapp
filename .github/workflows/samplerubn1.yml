name: first run
on:
  push:
    branches:
        - main
jobs:
    my_new_job:
        name: checkout
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v2

            - name: Print a message
              run: echo "Hello, GitHub Actions!"

     